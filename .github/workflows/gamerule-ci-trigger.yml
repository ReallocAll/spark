name: Gamerule CI Trigger

on:
  push:
    branches:
      - feat/gamerule-default-fallback

permissions:
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger PR validation"
